import { Routes } from '@angular/router';
import { AddProjectComponent } from './add-project.component';

export const ADDPROJECT_ROUTES: Routes = [
  { path: '', component: AddProjectComponent },
];
